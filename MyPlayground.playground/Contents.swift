// Math and Doubles Operations

(4+6) / 3 * 99

var age = 12;
var ageMin = 18;

if age < ageMin {
    let difference = ageMin - age;
    print(difference);
}


//transfer

var order = 2555.0565;
var cutRate : Float = 0.02;

var cut = Float(order) * Float(cutRate);
var cut2 = Double(order) * Double(cutRate);

